import { inviteUser, sendPasswordReset, updateUser } from '@/app/actions/users';
import { requireAdmin } from '@/lib/auth';
import { createClient } from '@/lib/supabase/server';

type ProfileRow={id:string;email:string;full_name:string|null;is_active:boolean;created_at:string;user_roles:{role:string}|Array<{role:string}>|null};
function roleOf(row:ProfileRow){return Array.isArray(row.user_roles)?row.user_roles[0]?.role:row.user_roles?.role}
export default async function UsersPage(){await requireAdmin(['super_admin']);const supabase=await createClient();const {data}=await supabase.from('profiles').select('id,email,full_name,is_active,created_at,user_roles(role)').order('created_at');const users=(data??[]) as unknown as ProfileRow[];return <main className="admin-content"><div className="admin-heading compact"><div><p className="eyebrow">Super Admin only</p><h1>User management</h1></div></div><section className="panel invite-panel"><div><h2>Undang user</h2><p>Undangan dikirim melalui email. Tidak tersedia registrasi publik.</p></div><form action={inviteUser}><label>Email<input type="email" name="email" required/></label><label>Role<select name="role" defaultValue="editor"><option value="editor">Editor</option><option value="viewer">Viewer</option><option value="super_admin">Super Admin</option></select></label><button className="primary-action">Kirim undangan</button></form></section><div className="content-table users-table"><div className="table-row table-head"><span>User</span><span>Role</span><span>Status</span><span>Sejak</span><span>Aksi</span></div>{users.map(user=><form action={updateUser} className="table-row" key={user.id}><input type="hidden" name="user_id" value={user.id}/><div><b>{user.full_name||'Belum mengisi nama'}</b><small>{user.email}</small></div><select name="role" defaultValue={roleOf(user)}><option value="super_admin">Super Admin</option><option value="editor">Editor</option><option value="viewer">Viewer</option></select><select name="is_active" defaultValue={String(user.is_active)}><option value="true">Aktif</option><option value="false">Nonaktif</option></select><time>{new Date(user.created_at).toLocaleDateString('id-ID')}</time><div className="inline-actions"><button>Simpan</button><button formAction={sendPasswordReset} name="email" value={user.email}>Reset</button></div></form>)}</div></main>}
